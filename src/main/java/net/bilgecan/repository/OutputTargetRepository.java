package net.bilgecan.repository;

import net.bilgecan.entity.OutputTarget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OutputTargetRepository extends JpaRepository<OutputTarget, Long> {
}
